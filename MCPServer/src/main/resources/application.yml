server:
    port: 8081

spring:
    application:
        name: mcpServer
    ai:
        mcp:
            server:
                name: webmvc-mcp-server
                version: 1.0.0
                type: SYNC
                instructions: "This server provides employee information tools and resources"
                capabilities:
                    tool: true
                    resource: true
                    prompt: true
                    completion: true

employee:
    service:
        url: http://localhost:50000
        api:
            key: "workshop-secret-key-2024"